//! Method, error and parameter types for the ResponsePlays endpoint.
#![allow(
    unused_imports,
)]
/* 
 * PagerDuty API
 *
 * This document describes the PagerDuty REST APIs.  For guides and examples please visit our [Documentation.](https://developer.pagerduty.com/docs/get-started/getting-started/)  Our REST APIs are defined in OpenAPI v3.x. You can view the schema at [github.com/PagerDuty/api-schema](https://github.com/PagerDuty/api-schema). 
 *
 * OpenAPI spec version: 2.0.0
 * Contact: support@pagerduty.com
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use serde::Deserialize;

use crate::models::*;

use std::collections::HashMap;
use serde_json::value::Value;

pub struct ResponsePlaysClient {
    pub(crate) api_endpoint: String,
    pub(crate) client: PagerDuty,
}



impl BaseOption for ResponsePlaysCreateResponsePlayParams {
    fn build_paginated_query_string(&self, pagination: PaginationQueryComponent) -> String {
        let mut query = form_urlencoded::Serializer::new(form_urlencoded::Serializer::Clone(&self.qs))
        query.append_pair("offset", &pagination.offset.to_string());
        query.append_pair("offset", &pagination.limit.to_string());
        query.finish()
    }
}

impl BaseOption for ResponsePlaysDeleteResponsePlayParams {
    fn build_paginated_query_string(&self, pagination: PaginationQueryComponent) -> String {
        let mut query = form_urlencoded::Serializer::new(form_urlencoded::Serializer::Clone(&self.qs))
        query.append_pair("offset", &pagination.offset.to_string());
        query.append_pair("offset", &pagination.limit.to_string());
        query.finish()
    }
}

impl BaseOption for ResponsePlaysGetResponsePlayParams {
    fn build_paginated_query_string(&self, pagination: PaginationQueryComponent) -> String {
        let mut query = form_urlencoded::Serializer::new(form_urlencoded::Serializer::Clone(&self.qs))
        query.append_pair("offset", &pagination.offset.to_string());
        query.append_pair("offset", &pagination.limit.to_string());
        query.finish()
    }
}
/// Query parameters for the [List Response Plays](ResponsePlays::list_response_plays()) endpoint.
#[derive(Default, Serialize)]
pub struct ResponsePlaysListResponsePlaysParams<'req> {
    pub(crate) qs: String,
}

pub struct ResponsePlaysListResponsePlaysParamsBuilder {
    qs: form_urlencoded::Serializer<'req, String>,
}

impl<'req> ResponsePlaysListResponsePlaysParamsBuilder<'req> {
    pub fn new() -> Self {
        Self {
            qs: form_urlencoded::Serializer::new(String::new())
        }
    }

    /// Filters the result, showing only the Response Plays whose name matches the query.
    pub fn query(&mut self, query: &'req str) -> &mut Self {
        self.qs.append_pair("query", serde_urlencoded::to_string(query));

        self
    }

    /// When this parameter is present, only those Response Plays that can be run manually will be returned.
    pub fn filter_for_manual_run(&mut self, filter_for_manual_run: bool) -> &mut Self {
        self.qs.append_pair("filter_for_manual_run", serde_urlencoded::to_string(filter_for_manual_run));

        self
    }
}


impl BaseOption for ResponsePlaysListResponsePlaysParams {
    fn build_paginated_query_string(&self, pagination: PaginationQueryComponent) -> String {
        let mut query = form_urlencoded::Serializer::new(form_urlencoded::Serializer::Clone(&self.qs))
        query.append_pair("offset", &pagination.offset.to_string());
        query.append_pair("offset", &pagination.limit.to_string());
        query.finish()
    }
}

impl BaseOption for ResponsePlaysRunResponsePlayParams {
    fn build_paginated_query_string(&self, pagination: PaginationQueryComponent) -> String {
        let mut query = form_urlencoded::Serializer::new(form_urlencoded::Serializer::Clone(&self.qs))
        query.append_pair("offset", &pagination.offset.to_string());
        query.append_pair("offset", &pagination.limit.to_string());
        query.finish()
    }
}

impl BaseOption for ResponsePlaysUpdateResponsePlayParams {
    fn build_paginated_query_string(&self, pagination: PaginationQueryComponent) -> String {
        let mut query = form_urlencoded::Serializer::new(form_urlencoded::Serializer::Clone(&self.qs))
        query.append_pair("offset", &pagination.offset.to_string());
        query.append_pair("offset", &pagination.limit.to_string());
        query.finish()
    }
}

impl ResponsePlaysClient {
    /// ---
    ///
    /// # Create a Response Play
    ///
    /// Creates a new Response Plays.
    /// 
    /// Response Plays allow you to create packages of Incident Actions that can be applied during an Incident's life cycle.
    /// 
    /// For more information see the [API Concepts Document](../../docs/CONCEPTS.md#response-plays)
    /// 
    /// 
    /// ---
    pub async fn create_response_play(&self, body: ResponsePlay) -> Result<ResponsePlayResponse, Error> {
        let uri = PagerDuty::parse_url(&self.api_endpoint, &self.path(), "")?;
            
        let req = self.client.build_request(
            uri,
            Builder::new().method(Method::POST),
            Some(PagerDuty::serialize_payload(ResponsePlay)?));


        self.client
            .process_into_value(req)
            .await
    }

    /// ---
    ///
    /// # Delete a Response Play
    ///
    /// Delete an existing Response Play. Once the Response Play is deleted, the action cannot be undone.
    /// 
    /// WARNING: When the Response Play is deleted, it is also removed from any Services that were using it.
    /// 
    /// Response Plays allow you to create packages of Incident Actions that can be applied to an Incident.
    /// 
    /// For more information see the [API Concepts Document](../../docs/CONCEPTS.md#response-plays)
    /// 
    /// 
    /// ---
    pub async fn delete_response_play(&self, id: &str) -> Result<(), Error> {
        let uri = PagerDuty::parse_url(&self.api_endpoint, format!("{}/{}", &self.path(), &id), "")?;
            
        let req = self.client.build_request(
            uri,
            Builder::new().method(Method::DELETE),
            Body::empty());


        self.client
            .process_into_value(req)
            .await
    }

    /// ---
    ///
    /// # Get a Response Play
    ///
    /// Get details about an existing Response Play.
    /// 
    /// Response Plays allow you to create packages of Incident Actions that can be applied during an Incident's life cycle.
    /// 
    /// For more information see the [API Concepts Document](../../docs/CONCEPTS.md#response-plays)
    /// 
    /// 
    /// ---
    pub async fn get_response_play(&self, id: &str) -> Result<ResponsePlayResponse, Error> {
        let uri = PagerDuty::parse_url(&self.api_endpoint, format!("{}/{}", &self.path(), &id), "")?;
            
        let req = self.client.build_request(
            uri,
            Builder::new().method(Method::GET),
            Body::empty());


        self.client
            .process_into_value(req)
            .await
    }

    /// ---
    ///
    /// # List Response Plays
    ///
    /// List all of the existing Response Plays.
    /// 
    /// Response Plays allow you to create packages of Incident Actions that can be applied during an Incident's life cycle.
    /// 
    /// For more information see the [API Concepts Document](../../docs/CONCEPTS.md#response-plays)
    /// 
    /// 
    /// ---
    pub async fn list_response_plays(&self, query_params: ResponsePlaysListResponsePlaysParams) -> Result<ResponsePlaysResponse, Error> {
        let uri = PagerDuty::parse_url(&self.api_endpoint, &self.path(), ResponsePlaysListResponsePlaysParamsBuilder::new().build().qs)?;
            
        let req = self.client.build_request(
            uri,
            Builder::new().method(Method::GET),
            Body::empty());


        self.client
            .process_into_value(req)
            .await
    }

    /// ---
    ///
    /// # Run a response play
    ///
    /// Run a specified response play on a given incident.
    /// 
    /// Response Plays are a package of Incident Actions that can be applied during an Incident's life cycle.
    /// 
    /// For more information see the [API Concepts Document](../../docs/CONCEPTS.md#response-plays)
    /// 
    /// 
    /// ---
    pub async fn run_response_play(&self, response_play_id: &str, body: RunResponsePlay) -> Result<RunResponsePlayResponse, Error> {
        let uri = PagerDuty::parse_url(&self.api_endpoint, format!("{}/{}", &self.path(), &response_play_id), "")?;
            
        let req = self.client.build_request(
            uri,
            Builder::new().method(Method::POST),
            Some(PagerDuty::serialize_payload(RunResponsePlay)?));


        self.client
            .process_into_value(req)
            .await
    }

    /// ---
    ///
    /// # Update a Response Play
    ///
    /// Updates an existing Response Play.
    /// 
    /// Response Plays allow you to create packages of Incident Actions that can be applied during an Incident's life cycle.
    /// 
    /// For more information see the [API Concepts Document](../../docs/CONCEPTS.md#response-plays)
    /// 
    /// 
    /// ---
    pub async fn update_response_play(&self, id: &str, body: ResponsePlay) -> Result<ResponsePlayResponse, Error> {
        let uri = PagerDuty::parse_url(&self.api_endpoint, format!("{}/{}", &self.path(), &id), "")?;
            
        let req = self.client.build_request(
            uri,
            Builder::new().method(Method::PUT),
            Some(PagerDuty::serialize_payload(ResponsePlay)?));


        self.client
            .process_into_value(req)
            .await
    }

}
